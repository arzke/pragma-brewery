import React from 'react'

const TemperatureTooHotNotification = ({ id }) => {
  return <span className='notification container-too-hot-notification'>↗ Container {id} is too hot</span>
}

export default TemperatureTooHotNotification
